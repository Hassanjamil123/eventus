class Ticket {
    constructor(title, eventId, qrCodeData, ticketId) {
      this.title = title;
      this.eventId = eventId;
      this.qrCodeData = qrCodeData;
      this.ticketId = ticketId;
      // Add any other relevant properties for the ticket
    }
  }
  
  export default Ticket;
   